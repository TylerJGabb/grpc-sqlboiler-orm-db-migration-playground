version: "3"
services:
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: db
    ports:
      - 5432:5432
    # volumes:
    #   # - ./seed.sql:/docker-entrypoint-initdb.d/seed.sql
    #   - ./1_create_table.sql:/docker-entrypoint-initdb.d/1_create_table.sql
    #   - ./2_insert_data.sql:/docker-entrypoint-initdb.d/2_insert_data.sql
